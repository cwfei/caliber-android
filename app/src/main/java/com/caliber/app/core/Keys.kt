package com.caliber.app.core

class Keys {
    enum class WatchEditor {
        TransitionName
    }

    class WatchViewer {
        companion object {
            const val watchId = "watchId"
            const val title = "title"
        }
    }
}
